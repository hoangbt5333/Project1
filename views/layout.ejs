<!DOCTYPE html>
<html lang="vi" class="h-full bg-slate-50">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: { sans: ['Inter', 'Segoe UI', 'system-ui', 'sans-serif'] },
          colors: { brand: { DEFAULT: '#2563eb', soft: '#1d4ed8' } }
        }
      }
    };
  </script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="h-full text-slate-900">
  <div class="min-h-screen flex bg-slate-50">
    <!-- Sidebar -->
    <aside class="w-72 bg-slate-900 text-slate-100 flex flex-col shadow-2xl fixed inset-y-0 left-0">
      <div class="px-5 py-6 border-b border-slate-800">
        <h1 class="text-lg font-semibold uppercase tracking-wide text-center">Quản lý SV</h1>
      </div>
      <nav class="flex-1 px-3 py-4 space-y-1">
        <a class="nav-item <%= currentPath === '/' ? 'is-active' : '' %>" href="/"><i class="fa-solid fa-home w-5"></i><span>Trang chủ</span></a>
        <a class="nav-item <%= currentPath.startsWith('/sinhvien') ? 'is-active' : '' %>" href="/sinhvien"><i class="fa-solid fa-user-graduate w-5"></i><span>Sinh viên</span></a>
        <a class="nav-item <%= currentPath.startsWith('/khoa') ? 'is-active' : '' %>" href="/khoa"><i class="fa-solid fa-building-columns w-5"></i><span>Khoa</span></a>
        <a class="nav-item <%= currentPath.startsWith('/lop') ? 'is-active' : '' %>" href="/lop"><i class="fa-solid fa-layer-group w-5"></i><span>Lớp</span></a>
        <a class="nav-item <%= currentPath.startsWith('/monhoc') ? 'is-active' : '' %>" href="/monhoc"><i class="fa-solid fa-book w-5"></i><span>Môn học</span></a>
        <a class="nav-item <%= currentPath.startsWith('/diem') ? 'is-active' : '' %>" href="/diem"><i class="fa-solid fa-chart-line w-5"></i><span>Quản lý điểm</span></a>
        <a class="nav-item <%= currentPath.startsWith('/attendance') ? 'is-active' : '' %>" href="/attendance"><i class="fa-solid fa-calendar-check w-5"></i><span>Điểm danh</span></a>
        <a class="nav-item <%= currentPath.startsWith('/chunhiem') ? 'is-active' : '' %>" href="/chunhiem"><i class="fa-solid fa-users w-5"></i><span>Chủ nhiệm</span></a>
        <a class="nav-item <%= currentPath.startsWith('/roles') ? 'is-active' : '' %>" href="/roles"><i class="fa-solid fa-user-shield w-5"></i><span>Vai trò</span></a>
        <a class="nav-item <%= currentPath.startsWith('/profile') ? 'is-active' : '' %>" href="/profile"><i class="fa-solid fa-file-alt w-5"></i><span>Tài khoản</span></a>
      </nav>
      <div class="px-4 pb-5 text-xs text-slate-400">
        © Bùi Trung Hoàng - 20235333
      </div>
    </aside>

    <!-- Main -->
    <div class="flex-1 flex flex-col min-h-screen ml-72">
      <header class="h-16 px-6 border-b border-slate-200 flex items-center justify-end bg-white/80 backdrop-blur sticky top-0 z-10">
        <% if (user) { %>
          <div class="flex items-center gap-3">
            <span class="inline-flex items-center gap-2 text-sm font-semibold text-slate-700">
              <i class="fa-solid fa-circle-user text-brand"></i>
              <%= user.username %>
            </span>
            <a href="/user/logout" class="inline-flex items-center gap-2 px-3 py-2 text-sm font-semibold text-brand bg-brand/10 hover:bg-brand/20 rounded-lg transition">
              <i class="fa-solid fa-arrow-right-from-bracket"></i>
              Đăng xuất
            </a>
          </div>
        <% } else { %>
          <div class="flex items-center gap-2">
            <a href="/user/login" class="px-4 py-2 text-sm font-semibold text-white bg-brand rounded-lg hover:bg-brand-soft transition">Đăng nhập</a>
            <a href="/user/register" class="px-4 py-2 text-sm font-semibold text-brand bg-white border border-brand rounded-lg hover:bg-brand/10 transition">Đăng ký</a>
          </div>
        <% } %>
      </header>

      <main class="flex-1 px-8 py-8 overflow-y-auto">
        <div class="max-w-7xl mx-auto space-y-8">
          <%- body %>
        </div>
      </main>

      <footer class="px-8 py-4 border-t border-slate-200 bg-white">
        <div class="max-w-7xl mx-auto flex justify-between text-sm text-slate-500">
          <span>Hệ thống quản lý sinh viên</span>
          <!-- <span>Phiên bản Tailwind UI</span> -->
        </div>
      </footer>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="/js/app.js"></script>
  <script src="/js/dashboard.js"></script>
</body>
</html>
